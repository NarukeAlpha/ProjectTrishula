# Archive

- `legacy-trading-poc-2026-08-30` is the complete local Trading workspace from before Project Trishula. Generated dependencies and build output remain on this machine but stay ignored by Git.
- `legacy-project-trishula-2024` is the tree from the previous `NarukeAlpha/ProjectTrishula` default branch before this migration.

Treat both snapshots as read-only history. New work belongs in the repository root.
